"use client";
import Link from "next/link";
import { Sidebar } from "@/components/Sidebar";
import { universities } from "@/lib/universities";
export default function ScholarshipsPage(){const rows=universities.filter(u=>u.scholarship && !/pending/i.test(u.scholarship));return <div className="min-h-screen bg-slate-50"><Sidebar/><main className="ml-64 min-h-screen"><header className="border-b bg-white px-8 py-6"><p className="text-sm font-medium text-blue-600">Funding</p><h1 className="mt-1 text-2xl font-semibold">Scholarships</h1><p className="mt-1 text-sm text-slate-500">Scholarship opportunities attached to your university records.</p></header><div className="grid gap-5 p-8 md:grid-cols-2">{rows.map(u=><article key={u.id} className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-4"><div><Link href={`/universities/${u.id}`} className="font-semibold text-blue-700 hover:underline">{u.name}</Link><p className="mt-1 text-xs text-slate-500">{u.country} · {u.major}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-medium text-blue-700">Scholarship</span></div><p className="mt-5 text-sm leading-6 text-slate-600">{u.scholarship}</p></article>)}</div></main></div>}
